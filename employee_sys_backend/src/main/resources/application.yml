spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/employee_sys
    username: root
    password: sr200316
mybatis:
  type-aliases-package: com.employee.bean
  mapper-locations: classpath:mapper/*.xml

server:
  port: 8081

# yml > yaml > properties